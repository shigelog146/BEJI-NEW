# AI Agent Instructions

This workspace is a small static website built from HTML files. There is no package manager, build system, or server configuration in the repository.

## Project structure

- `index.html` — main landing page
- `list.html` — list view page
- `buyer.html` — buyer entry page
- `buyer_history.html` — buyer history page
- `icon/` — icon assets

## What matters

- Preserve the existing static HTML structure.
- Do not introduce frontend frameworks, build tools, or external dependencies unless explicitly asked.
- Keep edits minimal and focused on the requested task.

## Formatting preference

- Use consistent HTML formatting and indentation.
- Prefer 2-space indentation for HTML, CSS, and JavaScript.
- Use double quotes for HTML attributes.
- Apply formatting on save or before completing edits when modifying existing files.

## When editing

- Respect the current document layout and whitespace where possible.
- If a requested change touches multiple pages, update only the relevant pages.
- Ask the user before making broader structural changes.

## No existing docs

- There is no README or project configuration to reference in the repository.
